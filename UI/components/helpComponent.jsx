import React from "react";
import { View, Text } from "react-native";

/**
 * 
 * Instructions to use Ophelia UI
 * 
 * Step 1: Find the host, the port number of Ophelia's backend will always be 6990.
 *          - Cannot be local since mobile devices cannot access localhost of the computer.
 *          - Perhaps create a dropdown menu with all available networks? Or allow manual input of IP address.
 *          - 
 * 
 */

const HelpComponent = () => {


  return (
    <View>
      <Text>Instructions</Text>
        <Text></Text>
    </View>
  );
};
export default HelpComponent;
